# Database — PostgreSQL connection string
# If using docker-compose.dev.yml: postgresql://postgres:postgres@localhost:5432/iridium
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iridium"

# Better Auth — session encryption secret (min 32 chars)
# Generate one: openssl rand -base64 32
BETTER_AUTH_SECRET=""

# Better Auth — base URL (must match where the app is running)
BETTER_AUTH_BASE_URL="http://localhost:5173"

# Vite-exposed copy for the client-side auth SDK
VITE_BETTER_AUTH_BASE_URL="http://localhost:5173"

# VoltAgent — separate Postgres for AI memory (keeps Prisma migrations clean)
VOLTAGENT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/voltagent"

# Anthropic — required for AI chat features
ANTHROPIC_API_KEY=""

# Linear API key for screenshot update comments
LINEAR_API_KEY=""

# Target Linear issue ID (UUID) for screenshot updates
LINEAR_ISSUE_ID=""

# Optional Git branch used for raw screenshot URLs (defaults to main)
SCREENSHOT_GITHUB_BRANCH="main"

# Runtime mode (set automatically by Bun/Node in most environments)
NODE_ENV="development"
